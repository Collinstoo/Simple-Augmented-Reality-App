import 'package:flutter/material.dart';

class WoodColorsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Wood Colors'),
      ),
      body: Center(
        child: Text('Wood Colors Screen'),
      ),
    );
  }
}
